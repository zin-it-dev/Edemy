import React from "react";
import { useParams } from "react-router";

import { type Outline } from "@/services/course.service";

const Outline: React.FC = () => {
  const { id } = useParams();

  return (
    <div>
      
    </div>
  );
};

export default Outline;
